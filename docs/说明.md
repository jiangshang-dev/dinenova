
fuintBackend 后端升级改造
技术栈：jdk21、认证spring security、优先用hutool

fuintBackend后端业务代码一行也不要少，只兼容改语法到jdk21
业务代码和原代码完全一样，jdk8的代码改成兼容jdk21代码；

代码规范：
封装获取当前用户的工具类，改造UserUtil 补充todo部分即可；
dinenova-common-core是fuintBackend的fuint-utils里面的
dinenova-framework对应fuint-framework
dinenova-repository对应fuint-repository
dinenova-servicey对应fuint-application的common包里面的service、dto、param、vo、domain，其他代码放在公共模块中，按阿里巴巴java开发手册好好规划一下结构

文件上传用阿里云oss和minio、rustfs、本地可切换，文件上传默认用s3
阿里云oss配置信息：
accessKey: LTAI5tAMhyQ5M4ax36oedKcQ
secretKey: DqOLunL4Qt8bWSxhi2mEktfcUKacVG
endpoint: oss-cn-beijing.aliyuncs.com
bucketName: mymianshioffer

示例：
dinenova:
    #配置文件存储服务类型，可选配置[local,aliyun,minio,rustfs]
    oss:
        type: aliyun
        accessKey: LTAI5tAMhyQ5M4ax36oedKcQ
        secretKey: DqOLunL4Qt8bWSxhi2mEktfcUKacVG
        endpoint: oss-cn-beijing.aliyuncs.com
        bucketName: mymianshioffer

在dinenova模块我该用的都加进来了，
其他的缺啥在补啥吧，
接口注解一律加@Debounce、@Operation、@Tag
fuint有注释的把注释也要加上
